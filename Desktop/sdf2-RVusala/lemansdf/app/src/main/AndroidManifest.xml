<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lemansdf"
        tools:targetApi="31">
        <activity
            android:name=".Activity.CartActivity"
            android:exported="false" />
        <!-- SplashScreen - Tətbiq başlandıqda ilk ekran -->
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- LoginScreen - Tətbiqdən sonra açılacaq səhifə -->
        <activity
            android:name=".LoginScreen"
            android:exported="true" /> <!-- MainActivity - Tətbiq açıldıqdan sonra açılacaq əsas səhifə -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>